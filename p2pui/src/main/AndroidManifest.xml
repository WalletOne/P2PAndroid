<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aronskiy_anton.p2pui" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application>
        <activity android:label="@string/bank_cards_activity_title" android:name=".bankcard.BankCardActivity"/>
        <activity android:label="@string/link_card_activity_title" android:name=".linkcard.LinkCardActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".bankcard.BankCardActivity" />
        </activity>

    </application>

</manifest>
